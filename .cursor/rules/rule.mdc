---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- This is a TypeScript Electron application which extract legal roms from the user Steam, GOG and etc, libraries
  - "/apps/electron" contains the host electron application, just a shell
  - "/core" is a ts lib - used by "web" - which contains most of the core logic for processing and extracting roms
  - "/web" it the UI vite/react application that builds to "dist" which is referenced by the electron app
- The web app uses tailwind css, shadcn, vite + react 19 and pnpm
- Ask and discuss before any significant change, unless told otherwise
- Under "/python" there are many reference implementation